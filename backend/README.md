# Backend

Planned stack:
- FastAPI
- SQLModel + Postgres
- Evaluation runner for golden CI/CD logs
- Provider interface (Fake provider by default)

In the next step, we will add a `/api/health` endpoint to verify the backend boots correctly.
